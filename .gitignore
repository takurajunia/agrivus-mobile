node_modules
.env
.expo/

# Firebase Admin SDK service account keys — server-side secrets, must never be committed.
# (google-services.json is fine to commit — it only holds public app identifiers.)
*firebase-adminsdk*.json
